(define (main args)
  (print '(1 2 3))
  0
)

(define justifique (lambda () 0))

(define acepte-deduccion (lambda (x) (x)))

(define responda (lambda () 0))

(define prueba-deduccion (lambda () 0))

(define arbol (lambda () 0))
